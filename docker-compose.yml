version: "3.8"

services:
  berg-agents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: berg-agents
    ports:
      - "2024:2024"
    volumes:
      - ./config:/app/config:ro
      - ./examples:/app/examples:ro
      - berg-agents-data:/data
    environment:
      - BERG_AGENT_PROVIDER=ollama
      - BERG_AGENT_OLLAMA_MODEL=gpt-oss:20b
      - BERG_AGENT_OLLAMA_BASE_URL=http://host.docker.internal:11434
      - BERG_AGENT_CHECKPOINT_DIR=/data/checkpoints
      - BERG_AGENT_SANDBOX_DIR=/data/sandboxes
      - BERG_AGENT_STREAM_ENABLED=true
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:2024/health" ]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  berg-agents-data:
    driver: local
